# subdir and objects in current dir
SUBDIRS	= func_a_file func_b_file
OBJECTS	= main.o
# CFLAGS+=${PROJECTPATH}/include


all:subdirs ${OBJECTS}

	
clean:cleansubdirs
	rm -f ${OBJECTS}
	
include ${MAKEINCLUDE}
